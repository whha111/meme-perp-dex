{
  "name": "memeperp-matching-engine",
  "version": "1.0.0",
  "description": "MemePerp DEX Matching Engine - V2 Peer-to-Peer Trading",
  "main": "server.ts",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run server.ts",
    "matching": "bun run server.ts",
    "test": "bun run test-runner.ts",
    "test:e2e": "bun run e2e-test.ts",
    "test:stress": "bun run stress-test.ts"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "dotenv": "^17.2.3",
    "viem": "^2.13.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
